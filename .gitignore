node_modules
dist
.astro
.cache

# Env files — keep .env.example committed, never the real ones
.env
.env.*
!.env.example

# Local OS noise
.DS_Store
Thumbs.db

# Editor
.vscode
.idea
.zed

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Source spreadsheets for the roadmap importer — real internal data, never
# committed. Root-anchored and named distinctly from src/modules/roadmap/import/
# (the importer's CODE, which must NOT be ignored).
/import-source/
